import { Configuration } from '../nox.js';

export default <Configuration>{
    packages: [],
    users: {
        saenai: {
            packages: [
                'kotlin'
            ],
        }
    },
}